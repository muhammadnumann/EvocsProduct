type CandidateSchedulePageProps = {
  id: number;
};

const EditCandidateSchedulePage = ({ id }: CandidateSchedulePageProps) => {
  // todo handle delete invalidating all currently set candidate schedules
  return <p>todo implement edit for {id}</p>;
};

export default EditCandidateSchedulePage;
