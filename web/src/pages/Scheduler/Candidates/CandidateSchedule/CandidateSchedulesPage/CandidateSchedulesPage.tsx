import { FC } from 'react';

import CandidateSchedulesCell from 'src/components/Scheduler/Candidates/CandidateSchedule/CandidateSchedulesCell';

const CandidateSchedulesPage: FC = () => {
  // todo embed page and pass candidateID
  // eslint-disable-next-line @typescript-eslint/ban-ts-comment
  // @ts-ignore
  return <CandidateSchedulesCell />;
};

export default CandidateSchedulesPage;
