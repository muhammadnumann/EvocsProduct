type ProductAuthLayoutProps = {
  children?: React.ReactNode;
};

const ProductAuthLayout = ({ children }: ProductAuthLayoutProps) => {
  return <>{children}</>;
};

export default ProductAuthLayout;
